# Point the SPA at your deployed API (from `cdk deploy` output "ApiUrl").
# If unset, the app fetches /config.json (written by CDK) at runtime.
# VITE_API_URL=https://abc123.execute-api.ap-southeast-2.amazonaws.com

# Force demo mode (no backend, seeded in-memory data):
# VITE_DEMO=1
